name: enterprise_ai_analytics
version: '1.0.0'
config-version: 2

profile: enterprise_ai_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  enterprise_ai_analytics:
    staging:
      +materialized: view
      +schema: STAGING
    marts:
      +materialized: table
      +schema: MARTS
    security:
      +materialized: table
      +schema: SECURITY
